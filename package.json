{
  "name": "booking-scheduler",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "bun run --cwd apps/api start:dev",
    "dev:web": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/api build && bun run --cwd apps/web build",
    "typecheck": "bun run --cwd apps/api typecheck && bun run --cwd apps/web typecheck",
    "db:migrate": "bun run --cwd apps/api migration:run",
    "db:migrate:revert": "bun run --cwd apps/api migration:revert",
    "verify:phase5": "node scripts/verify-phase5.mjs",
    "verify:phase6": "node scripts/verify-phase6.mjs",
    "test:api": "bun run --cwd apps/api test",
    "test:web": "bun run --cwd apps/web test",
    "test:e2e:install": "PLAYWRIGHT_BROWSERS_PATH=\"$HOME/Library/Caches/ms-playwright\" playwright install chromium",
    "test:e2e": "PLAYWRIGHT_BROWSERS_PATH=\"$HOME/Library/Caches/ms-playwright\" playwright test",
    "test:e2e:ui": "PLAYWRIGHT_BROWSERS_PATH=\"$HOME/Library/Caches/ms-playwright\" playwright test --ui",
    "test": "bun run test:api && bun run test:web"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
